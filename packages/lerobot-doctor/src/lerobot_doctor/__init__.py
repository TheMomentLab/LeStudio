"""lerobot-doctor: hardware setup and diagnostics for LeRobot.

Device discovery and udev port mapping, motor-ID setup, a live motor monitor,
and calibration validation. This package is a library today; the standalone
web UI and CLI arrive with the first release (see docs_public/direction.md).
"""

__version__ = "0.1.0"
